/**
 * Target display resolution for the Canopy dashboard (wall-mounted panel).
 * Kept for reference; layout is responsive and no longer locked to this size.
 */
export const TARGET_WIDTH = 1920;
export const TARGET_HEIGHT = 1080;
