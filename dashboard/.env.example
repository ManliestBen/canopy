# Copy to .env and fill in. For dev, Vite uses these when proxying to HA.
# For production, use the same names when running: npm run serve

VITE_HA_BASE_URL=https://your-instance.duckdns.org:8123
VITE_HA_TOKEN=your_long_lived_access_token

# For production server (server.js) only:
# HA_BASE_URL=https://your-instance.duckdns.org:8123
# HA_TOKEN=your_long_lived_access_token

# Optional: which Google Calendar(s) to show. Single ID or comma-separated (e.g. you@gmail.com or you@gmail.com,work@group.calendar.google.com). If unset, the first calendar shared with the service account is used.
# CALENDAR_ID=your@gmail.com
# CALENDAR_ID=you@gmail.com,work@group.calendar.google.com
