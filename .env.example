# Optional: used by ha-export/export-ha-api.js when run from repo root.
# Copy to .env and fill in. Keep each value on a single line (no line breaks inside the token).

# HA_BASE_URL=https://your-instance.duckdns.org:8123
# HA_TOKEN=your_long_lived_access_token
